#!/bin/bash 

printf "wait until the opsmgr service starts (about 5 minutes)\n"
printf "login to OM and configure the basic parameters\n"
printf "generate/get the agent info from the project (API keys)\n"
printf "edit the data/automation config file with the API key info to configure the agents\n"
printf "go on to Step 3\n"