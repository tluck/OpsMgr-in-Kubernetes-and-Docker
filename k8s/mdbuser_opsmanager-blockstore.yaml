---
apiVersion: mongodb.com/v1
kind: MongoDBUser
metadata:
  name: dbadmin-opsmanager-blockstore
spec:
  passwordSecretKeyRef:
    name: dbadmin-opsmanager-blockstore # Match to metadata.name of the User Secret
    key: password
  username: "dbAdmin"
  db: "admin" #
  mongodbResourceRef:
    name: "opsmanager-blockstore" # Match to MongoDB resource using authenticaiton
  roles:
    - db: "admin"
      name: "clusterAdmin"
    - db: "admin"
      name: "userAdminAnyDatabase"
    - db: "admin"
      name: "readWriteAnyDatabase"
    - db: "admin"
      name: "dbAdminAnyDatabase"

