---
apiVersion: mongodb.com/v1
kind: MongoDBUser
metadata:
  name: NAME-USER
spec:
  passwordSecretKeyRef:
    name: NAME-USER # Match to metadata.name of the User Secret
    key: password
  username: "USER"
  db: "admin" #
  mongodbResourceRef:
    name: "NAME" # Match to MongoDB resource using authentication
  roles:
    - db: "admin"
      name: "root"
    - db: "admin"
      name: "clusterAdmin"
    - db: "admin"
      name: "userAdminAnyDatabase"
    - db: "admin"
      name: "readWriteAnyDatabase"
    - db: "admin"
      name: "dbAdminAnyDatabase"

